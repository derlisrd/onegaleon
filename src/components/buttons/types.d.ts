import { ReactNode } from "react";
import { DimensionValue } from "react-native";

export interface buttonsTypes {
    children?: ReactNode,
    mt?: DimensionValue,
    my?: DimensionValue,
    mx?: DimensionValue,
    mb?: DimensionValue,
    pt?: DimensionValue,
    pb?: DimensionValue,
    px?: DimensionValue,
    py?: DimensionValue,
}