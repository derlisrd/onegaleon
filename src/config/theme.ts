import { colors } from "./colors";

export const themeLightColor = {
    primary: colors.primary,
    background: colors.gray[8],
    card: colors.gray[2],
    text: colors.white,
    border: colors.gray[1],
    notification: colors.gray[1],
};
