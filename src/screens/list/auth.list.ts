import { NavigatorScreenParams } from "@react-navigation/native"
import { BottomTapList } from "./bottom.tap.list"


export type AuthScreenList = {
    homebottomtap: NavigatorScreenParams<BottomTapList>,
    
}